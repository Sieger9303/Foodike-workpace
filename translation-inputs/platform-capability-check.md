# Platform Capability Check

## Purpose

This is the agent-maintained platform capability check. Before marking a critical/high platform capability as direct, replaced, fallback, or deferred, query CangjieSkills/HarmonyOS/Cangjie API docs, HarmonyOS-Examples, or package index lookup such as `https://pkg.cangjie-lang.cn` and record the evidence in `targetCapabilitySearch`. Direct means callable from the Cangjie target; ArkTS-only or native-only declarations are discovery evidence until an already planned or existing Cangjie wrapper, FFI, or bridge is verified with target build evidence. Use cjpm/project-management docs only to configure a chosen dependency.

## Status Values

`deferred`, `implemented-fallback`, `not-applicable`, `not-covered`, `planned`, `platform-replaced`, `unreviewed`, `verified-direct`

## Deferred Required Fields

`targetCapabilitySearch`, `blocker`, `targetFallback`, `futureCompletionPath`

## Entries

| capability id | status | phase | importance | decision | probe | capability | target API/library | strategy |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| media-library-access | not-applicable | phase-04-home-history | critical | resolved-not-applicable |  | Media library and album discovery | HarmonyOS Cangjie MediaLibraryKit / ohos.file.photo_access_helper (getPhotoAccessHelper, PhotoAccessHelper.getAlbums, album/media asset APIs) exists in the current SDK, but phase-04 home/history source behavior only consumes bundled drawable resources and does not browse user albums. | Treat the phase-04 media-library requirement as a source-analysis false positive. Implement home/history images from packaged app media resources only; do not add album discovery, gallery picker, or media permission scaffolding in this phase unless later source-backed behavior proves a real user-media flow. |
| file-access-and-saf | not-applicable | phase-05-detail-cart | critical | resolved-not-applicable |  | File access, document permissions, and share URIs | HarmonyOS Cangjie AbilityKit Want/URI APIs (`ohos.app.ability.want.Want`, `ohos.app.ability.want_constant.Flags`) exist for real cross-app URI/file sharing, but phase-05 detail/cart source behavior does not invoke them. | Treat the phase-05 file/document/share capability as not applicable for detail/cart. Keep the phase focused on detail/cart state and packaged-resource rendering; if a later source-backed share flow is implemented, route it through HarmonyOS Want-based sharing with explicit URI permission flags instead of introducing Android SAF assumptions. |
| image-viewer-gestures | not-applicable | phase-05-detail-cart | critical | resolved-not-applicable |  | Image viewer gestures and metadata | HarmonyOS Cangjie ArkUI `Image` and ImageKit `ohos.multimedia.image.createImageSource(String)` exist for real image display/processing, but phase-05 detail/cart source behavior only needs static packaged-image rendering. | Resolve the capability as not applicable for phase-05. Implement the detail/cart visuals with normal HarmonyOS Image rendering from packaged media; do not add zoom, reset, metadata, or EXIF UI unless a later source-backed requirement proves a real image viewer flow. |
| localization-resources | verified-direct | phase-01-project-skeleton-resources | high | resolved-real-api |  | Localized resources and formatted strings | HarmonyOS Cangjie LocalizationKit resource model (`ohos.base.ResourceStr`, `ohos.resource.AppResource`, `ohos.resource_manager.ResourceManager`) with standard HarmonyOS `string.json`/`color.json` resources | Phase 01 migrates Android labels, start-window colors, and resource-backed UI text into HarmonyOS descriptor/resource files, with AppResource helpers for Cangjie access where needed. Batch 3 also removes hard-coded route-shell labels by routing them through `foodike_resources.cj` and `app_router.cj` before display in `bootstrap_page.cj`. |
| runtime-permissions | not-applicable | phase-02-startup-session-navigation | high | resolved-not-applicable |  | Runtime permissions and protected platform access | HarmonyOS Cangjie AbilityKit permission APIs exist (`ohos.ability_access_ctrl.AtManager`, `ohos.security.permission_request_result.PermissionRequestResult`) but are not required by phase-02 startup/session/navigation source behavior. | Treat the phase-02 permission requirement as a source-analysis false positive. Do not add permission prompts or protected-access scaffolding during startup/session/navigation implementation unless later source-backed phases introduce real protected APIs. |
| background-and-platform-components | platform-replaced | phase-02-startup-session-navigation | high | resolved-platform-replacement |  | Background work, services, receivers, widgets, and notifications | HarmonyOS Cangjie AbilityKit app-entry APIs (`ohos.app.ability.ui_ability.UIAbility`, `UIAbilityContext`, `Want`) plus ArkUI `Navigation`/`NavPathStack` or `Router` for in-app startup and route flow | Resolve the source Android platform component surface as a HarmonyOS platform replacement: model app startup in the target `UIAbility`/descriptor entry, keep splash-like gating inside bootstrap state before rendering the main route, and implement onboarding/home navigation using the existing ArkUI route shell instead of Android `Application`, `ComponentActivity`, and `core-splashscreen` APIs. |
